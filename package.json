{
  "name": "vegan-challenge",
  "version": "1.0.0",
  "description": "webpage for the vegan challenge campaign",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "npx @biomejs/biome format ./src",
    "format": "npx @biomejs/biome format --write ./src",
    "postbuild": "sh -c 'cp -a dist/src/pages/. dist/ && rm -rf dist/src'"
  },
  "author": "ezinber",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "2.2.3",
    "cssnano": "^7.1.1",
    "glob": "^13.0.0",
    "postcss": "^8.5.6",
    "vite": "^5.4.7",
    "vite-plugin-html": "^3.2.2"
  },
  "dependencies": {
    "@formbricks/js": "^4.1.0"
  }
}
